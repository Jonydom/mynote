# Linux常用命令

[toc]

## tar命令

tar命令是**打包**命令，将一大堆文件或目录变成一个总的文件，便于压缩；tar命令生成的文件后缀是`.tar`。

**命令格式：**

```
tar[必要参数][选择参数][文件]
```

**命令功能：**

用来压缩和解压文件，tar本身不具有压缩功能，通过调用压缩功能实现的。

**命令参数：**

| 参数 | 参数说明                                                     |
| ---- | ------------------------------------------------------------ |
| -f   | 后面接要处理的文件（file）                                   |
| -c   | 新建打包文件（create）                                       |
| -x   | 要解压文件                                                   |
| -v   | 压缩或解压过程中，显示过程                                   |
| -j   | 通过bzip2方式压缩或解压，最后以.tar.br2 为后缀。压缩后大小小于.tar.gz |
| -z   | 通过gzip方式压缩或解压，最后以.tar.gz 为后缀                 |
| -t   | 查看打包文件中内容，重点文件名                               |

**常用命令：**

| 命令                                             | 命令说明                                 |
| ------------------------------------------------ | ---------------------------------------- |
| tar -jcv -f 压缩文件名称.tar.br2                 | 压缩方式一                               |
| tar -jxv -f 压缩文件名称.tar.br2 -C 指定文件目录 | 解压文件方式一                           |
| tar -zcv -f 压缩文件名称.tar.gz                  | 压缩方式二                               |
| tar -zxv -f 压缩文件名称.tar.gz                  | 解压文件方式二                           |
| tar -tf 压缩文件名                               | 查看文件名                               |
| tar -tvf 压缩文件                                | 查看文件：所属权限、用户名用户组、日期等 |

## ls命令

## export命令

**命令格式：**

```
export [-fnp][变量名称] = [变量设置值]
```

**命令功能：**

用于设置或显示环境变量。

**命令参数：**

| 参数 | 参数功能                                                     |
| ---- | ------------------------------------------------------------ |
| -f   | 代表[变量名称]中为函数名称                                   |
| -n   | 删除指定的变量。变量实际上并未删除，只是不会输出到后续指令的执行环境中 |
| -p   | 列出所有的shell赋予程序的环境变量                            |

## mkdir命令

**命令格式：**

```
mkdir [-pm] [目录名称]
```

**命令功能：**

创建目录（文件夹）的命令。

**命令参数：**

| 参数 | 参数功能                                                     |
| ---- | ------------------------------------------------------------ |
| -p   | 递归生成不存在的父目录或在创建已存在的目录时不报错（parent） |
| -m   | 在创建目录时设置权限（mode）                                 |

## **insmod命令**

**命令功能：**

install module的缩写，用来载入模块，通过模式的方式在需要时载入内核，可使内核精简，高效。此类载入的模块，通常为设备驱动程序。（动态加载）

**命令格式：**

```
insmod [参数] 模块名
```

- **insmod与modprobe命令**

  在linux中，insmod和modprobe都可以用来加载module，但是更推荐modprobe。

  1. modprobe可以解决load module时的依赖关系，而insmod不能；
  2. modprobe默认会去/lib/modules/目录下面查找module，而insmod只在给它的参数中去找module（默认在当前目录找）

![img](https://raw.githubusercontent.com/Jonydom/myPic/main/img/202201261039558.png)

## dmesg命令

**命令功能：**

dmesg命令用于在Unix-like系统中显示内核的相关信息（display message）

**命令参数：**

| 参数            | 参数功能                                |
| --------------- | --------------------------------------- |
| -c              | 显示信息后，清除ring buffer中的内容     |
| -s <缓冲区大小> | 预设置为8196，刚好等于ring buffer的大小 |
| -n              | 设置记录信息的层级                      |

## mknod命令

**命令功能：**

mknod命令用于创建字符设备文件和块设备文件。

**命令格式：**

```
mknod [选项][文件名称][文件类型][主设备号][次设备号]
```

**命令参数：**

- 选项

  | 参数      | 参数说明         |
  | --------- | ---------------- |
  | -Z        | 设置安全的上下文 |
  | -m        | 设置权限模式     |
  | -help     | 显示帮助信息     |
  | --version | 显示版本信息     |

- 文件类型

  b表示块设备，c表示字符设备

  
